name: 🚀 Deploy - RG POS

on:
  push:
    branches: [ master ]
    paths-ignore:
      - 'docs/**'
      - '*.md'
  workflow_dispatch:
    inputs:
      deploy_frontend:
        description: 'Deploy Frontend'
        required: false
        default: true
        type: boolean
      deploy_backend:
        description: 'Deploy Backend'
        required: false
        default: true
        type: boolean

jobs:
  deploy_backend:
    name: Deploy Backend - RG POS
    runs-on: ubuntu-latest
    environment: rgposdemo
    if: ${{ github.event.inputs.deploy_backend != 'false' || github.event_name == 'push' }}
    
    steps:
    - name: Setup Environment
      id: env-setup
      run: |
        # Sanitize variables to remove trailing newlines
        echo "backend_path=$(echo "${{ secrets.CLIENT_RGPOS_BACKEND_DEPLOY_PATH || secrets.BACKEND_DEPLOY_PATH }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_host=$(echo "${{ secrets.CLIENT_RGPOS_VPS_HOST || secrets.VPS_HOST }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_port=$(echo "${{ secrets.CLIENT_RGPOS_VPS_PORT || secrets.VPS_PORT }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_user=$(echo "${{ secrets.CLIENT_RGPOS_VPS_USERNAME || secrets.VPS_USERNAME }}" | xargs)" >> $GITHUB_OUTPUT

    - name: Deploy Backend to VPS
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ steps.env-setup.outputs.vps_host }}
        port: ${{ steps.env-setup.outputs.vps_port }}
        username: ${{ steps.env-setup.outputs.vps_user }}
        key: ${{ secrets.CLIENT_RGPOS_VPS_SSH_KEY || secrets.VPS_SSH_KEY }}
        script: |
          cd ${{ steps.env-setup.outputs.backend_path }}
          echo "🚀 Deploying backend to RG POS..."
          git fetch origin
          git reset --hard origin/master
          cd apps/backend
          
          # Detectar usuario del servidor web
          WEB_USER="www-data"
          if id nginx >/dev/null 2>&1; then
            WEB_USER="nginx"
          elif id apache >/dev/null 2>&1; then
            WEB_USER="apache"
          fi
          
          # Detectar usuario del sitio (para CyberPanel/cPanel)
          SITE_DIR=$(dirname "${{ steps.env-setup.outputs.backend_path }}")
          SITE_USER=$(stat -c '%U' "$SITE_DIR" 2>/dev/null || echo "")
          if [ -n "$SITE_USER" ] && [ "$SITE_USER" != "root" ]; then
            WEB_USER="$SITE_USER"
            echo "✅ Usuario del sitio detectado: $WEB_USER"
          fi
          
          # Asegurar permisos de storage y cache antes de cualquier operación
          mkdir -p storage/framework/views
          mkdir -p storage/framework/cache/data
          mkdir -p storage/framework/sessions
          mkdir -p storage/logs
          mkdir -p bootstrap/cache
          mkdir -p storage/app/public/system/logos
          mkdir -p storage/app/public/system/favicons
          mkdir -p public/images
          
          # Configurar permisos
          chmod -R 775 storage bootstrap/cache 2>/dev/null || sudo chmod -R 775 storage bootstrap/cache || true
          chmod -R 777 storage/framework/views 2>/dev/null || sudo chmod -R 777 storage/framework/views || true
          chmod -R 777 storage/framework/cache/data 2>/dev/null || sudo chmod -R 777 storage/framework/cache/data || true
          chmod -R 775 storage/app/public 2>/dev/null || sudo chmod -R 775 storage/app/public || true
          chmod -R 755 public/images 2>/dev/null || sudo chmod -R 755 public/images || true
          chown -R $WEB_USER:$WEB_USER storage bootstrap/cache public/images 2>/dev/null || sudo chown -R $WEB_USER:$WEB_USER storage bootstrap/cache public/images || true
          
          composer install --no-dev --optimize-autoloader
          php artisan config:cache
          php artisan route:cache
          php artisan view:cache
          php artisan migrate --force
          
          # Crear symlink para /images/ (LiteSpeed compatibility)
          cd ../../..
          if [ ! -L "images" ] && [ ! -d "images" ]; then
            ln -s apps/backend/public/images images
            echo "✅ Symlink /images/ creado"
          elif [ -L "images" ]; then
            echo "✅ Symlink /images/ ya existe"
          else
            echo "⚠️  Directorio /images/ existe (no es symlink), omitiendo"
          fi
          
          echo "✅ Backend deployment completed"

          
    - name: Notify backend deployment
      if: always()
      run: |
        echo "✅ Backend deployment completed for RG POS"

  deploy_frontend:
    name: Deploy Frontend - RG POS
    runs-on: ubuntu-latest
    environment: rgposdemo
    if: ${{ github.event.inputs.deploy_frontend != 'false' || github.event_name == 'push' }}
    
    steps:
    - name: Setup Environment
      id: env-setup
      run: |
        # Sanitize variables to remove trailing newlines
        echo "frontend_path=$(echo "${{ secrets.CLIENT_RGPOS_FRONTEND_DEPLOY_PATH || secrets.FRONTEND_DEPLOY_PATH }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_host=$(echo "${{ secrets.CLIENT_RGPOS_VPS_HOST || secrets.VPS_HOST }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_port=$(echo "${{ secrets.CLIENT_RGPOS_VPS_PORT || secrets.VPS_PORT }}" | xargs)" >> $GITHUB_OUTPUT
        echo "vps_user=$(echo "${{ secrets.CLIENT_RGPOS_VPS_USERNAME || secrets.VPS_USERNAME }}" | xargs)" >> $GITHUB_OUTPUT
        echo "api_url=$(echo "${{ secrets.CLIENT_RGPOS_API_URL || secrets.VITE_API_URL }}" | xargs)" >> $GITHUB_OUTPUT

    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: apps/frontend/package-lock.json
        
    - name: Install dependencies
      run: |
        cd apps/frontend
        npm ci || npm install --force
        npm install @rollup/rollup-linux-x64-gnu --save-optional || true
        npm install @swc/core-linux-x64-gnu --save-optional || true
        npm install lightningcss-linux-x64-gnu --save-optional || true
        npm install @tailwindcss/oxide-linux-x64-gnu --save-optional || true
        
    - name: Build frontend
      run: |
        cd apps/frontend
        npm run build
      env:
        VITE_API_URL: ${{ steps.env-setup.outputs.api_url }}
        VITE_FEATURES: ${{ secrets.CLIENT_RGPOS_FEATURES_JSON }}
        
    - name: Deploy Frontend to VPS
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ steps.env-setup.outputs.vps_host }}
        port: ${{ steps.env-setup.outputs.vps_port }}
        username: ${{ steps.env-setup.outputs.vps_user }}
        key: ${{ secrets.CLIENT_RGPOS_VPS_SSH_KEY || secrets.VPS_SSH_KEY }}
        source: "apps/frontend/dist/*"
        target: ${{ steps.env-setup.outputs.frontend_path }}
        strip_components: 2
        rm: false
        
    - name: Verify and fix frontend deployment
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ steps.env-setup.outputs.vps_host }}
        port: ${{ steps.env-setup.outputs.vps_port }}
        username: ${{ steps.env-setup.outputs.vps_user }}
        key: ${{ secrets.CLIENT_RGPOS_VPS_SSH_KEY || secrets.VPS_SSH_KEY }}
        script: |
          DEPLOY_PATH="${{ steps.env-setup.outputs.frontend_path }}"
          echo "🔍 Verificando deployment en: $DEPLOY_PATH"
          
          # Si los archivos están en dist/, moverlos a la raíz
          if [ -d "$DEPLOY_PATH/dist" ]; then
            echo "⚠️  Archivos encontrados en subdirectorio dist/, moviendo a raíz..."
            
            # Mover index.html
            if [ -f "$DEPLOY_PATH/dist/index.html" ]; then
              mv "$DEPLOY_PATH/dist/index.html" "$DEPLOY_PATH/"
            fi
            
            # Mover assets (eliminando el anterior para evitar anidamiento)
            if [ -d "$DEPLOY_PATH/dist/assets" ]; then
              rm -rf "$DEPLOY_PATH/assets"
              mv "$DEPLOY_PATH/dist/assets" "$DEPLOY_PATH/"
            fi
            
            # Mover cualquier otro archivo/directorio restante
            if [ -d "$DEPLOY_PATH/dist" ]; then
              shopt -s dotglob
              mv "$DEPLOY_PATH/dist"/* "$DEPLOY_PATH/" 2>/dev/null || true
              shopt -u dotglob
            fi
            
            # Eliminar directorio dist/ vacío o remanente
            rm -rf "$DEPLOY_PATH/dist"
            echo "✅ Archivos movidos a la raíz"
          fi
          
          # Verificar que index.html existe
          if [ ! -f "$DEPLOY_PATH/index.html" ]; then
            echo "❌ ERROR: index.html no encontrado!"
            echo "📋 Contenido del directorio:"
            ls -la "$DEPLOY_PATH" || true
            exit 1
          fi
          
          # Asegurar permisos correctos
          WEB_USER="www-data"
          if id nginx >/dev/null 2>&1; then
            WEB_USER="nginx"
          elif id apache >/dev/null 2>&1; then
            WEB_USER="apache"
          fi
          
          # Detectar usuario de CyberPanel si existe
          if [ -d "/home" ]; then
            SITE_DIR=$(dirname "$DEPLOY_PATH" 2>/dev/null || echo "")
            if [ -n "$SITE_DIR" ]; then
              SITE_USER=$(stat -c '%U' "$SITE_DIR" 2>/dev/null || echo "")
              if [ -n "$SITE_USER" ] && [ "$SITE_USER" != "root" ]; then
                WEB_USER="$SITE_USER"
                echo "✅ Usuario detectado: $WEB_USER"
              fi
            fi
          fi
          
          chmod -R 755 "$DEPLOY_PATH" 2>/dev/null || sudo chmod -R 755 "$DEPLOY_PATH" || true
          find "$DEPLOY_PATH" -type f -exec chmod 644 {} \; 2>/dev/null || sudo find "$DEPLOY_PATH" -type f -exec chmod 644 {} \; || true
          chown -R $WEB_USER:$WEB_USER "$DEPLOY_PATH" 2>/dev/null || sudo chown -R $WEB_USER:$WEB_USER "$DEPLOY_PATH" || true
          
          echo "✅ Verificación completada. Archivos en:"
          ls -lah "$DEPLOY_PATH" | head -10
          echo "✅ index.html existe: $([ -f "$DEPLOY_PATH/index.html" ] && echo 'SÍ' || echo 'NO')"
        
    - name: Notify frontend deployment
      if: always()
      run: |
        echo "✅ Frontend deployment completed for RG POS"

# Job notify eliminado para ahorrar minutos de GitHub Actions
